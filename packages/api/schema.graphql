type Configuration {
    id: ID!
    name: String
    url: String
}

type Query {
    Configurations: [Configuration]
}

type Mutation {
    createConfiguration(name: String, url: String): Configuration
}